<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="450" d:DesignHeight="550"
        Width="450" Height="550"
        CanResize="False"
        WindowStartupLocation="CenterScreen"
        x:Class="UnityProxyLauncher.MainWindow"
        Title="GlobalUnityInstaller"
        Background="#1e1e1e">

    <Grid RowDefinitions="Auto, Auto, Auto, Auto, Auto, *" Margin="30">
        
        <TextBlock Grid.Row="0" Text="GlobalUnityInstaller" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,30" />

        <!-- Path Selection -->
        <StackPanel Grid.Row="1" Spacing="10" Margin="0,0,0,20">
            <Label Content="Unity Hub 路径" Foreground="#aaaaaa" FontSize="12"/>
            <Grid ColumnDefinitions="*, 10, Auto">
                <TextBox Grid.Column="0" Name="PathTextBox" Watermark="正在自动查找..." IsReadOnly="True" />
                <Button Grid.Column="2" Name="SelectPathButton" Content="选择文件夹" />
            </Grid> 
            <TextBlock Text="留空则自动查找，或点击按钮手动选择" Foreground="#666666" FontSize="11" />
        </StackPanel>

        <!-- Proxy Config -->
        <StackPanel Grid.Row="2" Spacing="8" Margin="0,0,0,15">
            <Label Content="HTTP 代理端口" Foreground="#aaaaaa" FontSize="12"/>
            <TextBox Name="HttpPortTextBox" Watermark="例如: 7890" />
        </StackPanel>

        <StackPanel Grid.Row="3" Spacing="8" Margin="0,0,0,30">
            <Label Content="SOCKS5 代理端口" Foreground="#aaaaaa" FontSize="12"/>
            <TextBox Name="SocksPortTextBox" Watermark="例如: 1080" />
        </StackPanel>

        <!-- Actions -->
        <Button Grid.Row="4" Name="LaunchButton" Content="🚀 启动 Unity Hub" 
                HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                Background="#2e7d32" Foreground="White" FontSize="16" Height="45" />

        <!-- Status Bar -->
        <Border Grid.Row="5" Name="StatusBorder" Margin="0,20,0,0" CornerRadius="4" Padding="10" IsVisible="False">
            <TextBlock Name="StatusTextBlock" TextAlignment="Center" FontSize="12" TextWrapping="Wrap" />
        </Border>

    </Grid>
</Window>
